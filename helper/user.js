const User = require("../models/user")

module.exports = {
    readUser: () => {},
    toCreateUser: () => {},
    createUser: async (req, res)=> {
        try {
            
        } catch (e) {
            console.log(e)
        }
    },
    toUpdateUser: () => {},
    updateUser: () => {},
    deleteUser: () => {},
}